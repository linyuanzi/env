package main

import (
	"fmt"
	// "os"
	// "strings"
)

func main() {
	// path, _ := os.LookupEnv("PATH")
	// fmt.Printf("test path= %v\n", path)
	// a := "test,test1,test2"
	// str_arr := strings.Split(a, ",")
	// for _, str := range str_arr {
	// 	println(str)
	// }
	var a string
	if a == "" {
		fmt.Println("empty")
	}

}
